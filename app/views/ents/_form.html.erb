<%= form_with(model: ent) do |form| %>
  <% if ent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ent.errors.count, "error") %> prohibited this ent from being saved:</h2>

      <ul>
        <% ent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="field">
    <%= form.label :reviewid %>
    <%= form.text_field :reviewid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
